{
  "name": "@saas/pricing",
  "version": "0.1.0",
  "private": true,
  "description": "Motor de tramos graduated, redondeo half-up y enum Currency. Funciones puras importadas por backend y frontend: no existen dos implementaciones que puedan divergir (referencia 10).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
